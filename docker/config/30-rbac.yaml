rbac:
  token_max_expiration: 24h

  # Automatically discover user accounts from secret files in the secrets directory.
  # Scans for matching pairs: <name>-id-1.pub and <name>-sk-1.nk
  auto_accounts_dir: '{{ env "IAM_SECRETS_DIR" }}'

  user_accounts: []

  role_binding_matching_strategy: best_match
  role_binding: []
    # - match: { claim: preferred_username, value: 'admin' }
    #   user_account: XX
    #   token_max_expiration: 1h
    #   roles:
    #     - can-api
    #     - streaming

  roles: []
    # - name: can-api
    #   permissions:
    #     pub:
    #       allow:
    #         - basic.api.>
    #     sub:
    #       allow:
    #         - basic.api.>

    # - name: streaming
    #   permissions:
    #     sub:
    #       allow:
    #         - "$JS.API.>"
    #         - "_INBOX.>"
    #     pub:
    #       allow:
    #         - "$JS.API.STREAM.CREATE.basic_test_stream"
    #     resp:
    #       max_msgs: 1
    #       exp: 1h
    #   limits:
    #     data: 65536

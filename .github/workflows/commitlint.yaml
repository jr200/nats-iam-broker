name: commitlint

on:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: read

jobs:
  commitlint:
    uses: jr200-labs/github-action-templates/.github/workflows/lint_commits.yaml@master

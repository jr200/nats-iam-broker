# GENERATED/SHARED WORKFLOW: copied into consuming repos by sync-shared.
# Do not edit in consuming repos; change jr200-labs/github-action-templates and sync.
name: ci-go

on:
  pull_request:
  # Seed setup-go's default-branch cache so pull-request branches can restore
  # it. GitHub cache scope prevents one feature branch from warming another.
  push:
    branches: [master]
  workflow_dispatch:
    inputs:
      runner:
        description: Runner pool
        required: false
        type: choice
        default: configured
        options:
          - configured
          - self-hosted
          - github-hosted

permissions:
  contents: read
  pull-requests: read

jobs:
  ci:
    uses: jr200-labs/github-action-templates/.github/workflows/ci_go.yaml@master
    with:
      runner: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.runner == 'self-hosted' && 'self-hosted' || github.event_name == 'workflow_dispatch' && github.event.inputs.runner == 'github-hosted' && 'ubuntu-latest' || fromJSON(vars.RUNNER_PROFILES)[vars.RUNNER_PROFILE].default }}
